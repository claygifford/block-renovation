import {codingExercise} from './coding-exercise.js';

const items = ['red', 'blue', 'red', 'green', 'blue'];
const order = ['blue', 'red', 'black'];

console.log(`Example input:
items = [“red”, “blue”, “red”, “green”, “blue”] // Length = n
order = [“blue”, “red”, “black”] // Length = k`)

console.log(`Output: ${codingExercise(items, order)}`);